[project]
name = "openai-test"
version = "0.1.0"
description = "Azure OpenAI sample managed with uv"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "openai>=1.0.0",
]

[tool.uv]
package = false
